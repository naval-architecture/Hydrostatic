# Copy to .env and adjust as needed.

# Frontend (Next.js) — must be prefixed NEXT_PUBLIC_ to be available client-side
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend (FastAPI)
NAVAL_ARCH_CORS_ORIGINS=["http://localhost:3000"]
